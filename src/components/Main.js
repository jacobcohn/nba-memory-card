import React from "react";
import styled from "styled-components";

const Main = () => {
  return (
    <MainWrapper>
      <h1>Main</h1>
    </MainWrapper>
  );
};

const MainWrapper = styled.div`
  flex: 1;
`;

export default Main;
